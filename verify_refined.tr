(print "Verifying Refined Tree String...")
(defun k (x y) x)

(print "Primitive *:")
;; Should be "atom" or similar, not "n"
(print (tree-string '*))

(print "Compiled K:")
;; K = (n n)
(print (tree-string (compile 'k)))

(print "Compiled (* x x):")
(defun square (x) (* x x))
;; Expected: (n ((n atom) (n n))) or similar
(print (tree-string (compile 'square)))
